NEWSAPI_KEY=
CRON_SECRET=
DATABASE_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
NEXTAUTH_URL=
BRAVE_SEARCH_API_KEY=
ANTHROPIC_API_KEY=
# Owner email shown in the account menu (single-user mode). Server-only — never
# hardcode it in source. The deployment should also sit behind Vercel password
# protection since the auth system is currently disabled.
OWNER_EMAIL=
